{
    "config": {
        "flow_title": "{name} ({address})",
        "step": {
            "bluetooth_confirm": {
                "title": "Set up Chef iQ probe",
                "description": "Add **{name}** (`{address}`) to Home Assistant?"
            },
            "user": {
                "title": "Set up Chef iQ probe",
                "description": "Choose the Chef iQ probe to add.",
                "data": {
                    "address": "Bluetooth device"
                }
            }
        },
        "abort": {
            "no_devices_found": "No Chef iQ probes were found nearby. Wake the probe (touch the button) and ensure at least one Bluetooth source — local USB, ESPHome BT proxy, SLZB-06, etc. — is in range, then try again.",
            "already_configured": "This Chef iQ probe is already configured."
        }
    },
    "entity": {
        "sensor": {
            "tip_temperature": { "name": "Tip" },
            "ring_1_temperature": { "name": "Ring 1" },
            "ring_2_temperature": { "name": "Ring 2" },
            "ambient_temperature": { "name": "Ambient" },
            "battery": { "name": "Battery" },
            "rssi": { "name": "Signal strength" }
        }
    }
}
